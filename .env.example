# Server (server/.env or export before starting server)
OPENAI_API_KEY=your-openai-api-key-here
PORT=3001

# Client (client/.env for production build when API is on another host)
# VITE_API_URL=http://localhost:3001
